--- a/webview/assets/app-initial-236e1501144c.js
+++ b/webview/assets/app-initial-236e1501144c.js
@@ -48761,7 +48761,8 @@
           handler: (e, t) => {
             let { nativeEvent: n } = e,
               { onActivation: r } = t;
-            return !n.isPrimary || n.button !== 0
+            return !n.isPrimary || n.button !== 0 ||
+              (n.pointerType === `touch` && n.target instanceof Element && n.target.closest(`.app-shell-left-panel`))
               ? !1
               : (r?.({ event: n }), !0);
           },
@@ -50682,5 +50683,9 @@
                   ? e.onContextMenu
                   : is(e.onContextMenu, (e) => {
+                      if (e.nativeEvent.pointerType === `touch` && e.target instanceof Element && e.target.closest(`.app-shell-left-panel`)) {
+                        e.preventDefault();
+                        return;
+                      }
                       (u(), d(e), e.preventDefault());
                     }),
                 onPointerDown: r
@@ -50690,6 +50695,8 @@
                   : is(
                       e.onPointerDown,
                       CMe((e) => {
+                        // Sidebar touch gestures scroll; explicit action buttons open menus.
+                        if (e.pointerType === `touch` && e.target instanceof Element && e.target.closest(`.app-shell-left-panel`)) return;
                         (u(), (l.current = window.setTimeout(() => d(e), 700)));
                       }),
                     ),
--- a/webview/assets/app-primary-6b28e06666ff.js
+++ b/webview/assets/app-primary-6b28e06666ff.js
@@ -139496,7 +139496,29 @@
   let l;
   return (
     t[8] !== a || t[9] !== c
-      ? ((l = (0, gX.jsx)(`div`, { className: _X, style: a, children: c })),
+      ? ((l = (0, gX.jsxs)(`div`, {
+          className: _X,
+          style: a,
+          children: [
+            (0, gX.jsx)(`button`, {
+              type: `button`,
+              className: `codex-web-sidebar-more`,
+              "aria-label": r === `归档聊天` ? `更多会话操作` : `More chat actions`,
+              "aria-haspopup": `menu`,
+              onClick: (e) => {
+                e.preventDefault();
+                e.stopPropagation();
+                const box = e.currentTarget.getBoundingClientRect();
+                e.currentTarget.dispatchEvent(new MouseEvent(`contextmenu`, {
+                  bubbles: true, cancelable: true, button: 2,
+                  clientX: box.left, clientY: box.bottom,
+                }));
+              },
+              children: `⋯`,
+            }),
+            c,
+          ],
+        })),
         (t[8] = a),
         (t[9] = c),
         (t[10] = l))
@@ -139621,6 +139643,24 @@
   return (
     t[8] !== c || t[9] !== l || t[10] !== i
-      ? ((u = (0, yX.jsxs)(`div`, { className: c, children: [l, i] })),
+      ? ((u = (0, yX.jsxs)(`div`, {
+          className: c,
+          children: [l, i ?? (0, yX.jsx)(`button`, {
+            type: `button`,
+            className: `codex-web-sidebar-more`,
+            "aria-label": document.documentElement.lang.startsWith(`zh`) ? `更多会话操作` : `More chat actions`,
+            "aria-haspopup": `menu`,
+            onClick: (e) => {
+              e.preventDefault();
+              e.stopPropagation();
+              const box = e.currentTarget.getBoundingClientRect();
+              e.currentTarget.dispatchEvent(new MouseEvent(`contextmenu`, {
+                bubbles: true, cancelable: true, button: 2,
+                clientX: box.left, clientY: box.bottom,
+              }));
+            },
+            children: `⋯`,
+          })],
+        })),
         (t[8] = c),
         (t[9] = l),
         (t[10] = i),
